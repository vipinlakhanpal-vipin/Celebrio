// Bump this on every meaningful change you ship. The bottom nav's refresh
// button (mobile) and the version badge (desktop) both compare the version
// baked into the currently-loaded page against /api/version (which always
// reflects whatever is actually deployed) to tell the user a refresh is
// available.
export const APP_VERSION = "1.19";
